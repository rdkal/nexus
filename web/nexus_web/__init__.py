"""nexus-web: the optional Python/iris web UI for nexus.

A normal nexus project — added by the operator, deployed like any other — that
connects to the daemon's Unix socket and serves the public HTTP interface.
"""
